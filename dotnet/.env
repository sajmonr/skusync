# Single source of truth for the local Postgres credentials. Docker Compose auto-loads this
# file and interpolates the values into compose.yaml (postgres env + both connection strings),
# so changing a value here keeps every service in sync. Local dev only — not real secrets.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=skusync
